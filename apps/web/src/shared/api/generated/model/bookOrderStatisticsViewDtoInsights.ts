/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * book-nest API
 * REST API for the book-nest project
 * OpenAPI spec version: 1.0
 */
import type { BookOrderStatisticsViewDtoInsightsBooksItem } from "./bookOrderStatisticsViewDtoInsightsBooksItem";
import type { BookOrderStatisticsViewDtoInsightsOrdersItem } from "./bookOrderStatisticsViewDtoInsightsOrdersItem";
import type { BookOrderStatisticsViewDtoInsightsSpendByCurrencyItem } from "./bookOrderStatisticsViewDtoInsightsSpendByCurrencyItem";

/**
 * Insights grouped by the context that is selected on the page. Spend is grouped per currency so a page showing UAH never receives a EUR record, and the count metrics have their own groups so switching the chart switches the insights with it.
 */
export type BookOrderStatisticsViewDtoInsights = {
  /** One already selected and ordered group of insights. The backend owns the business selection: the array is short, ranked best first, and holds at most one signal of each family, so the frontend renders it as it arrives and never re-ranks or truncates it. */
  books: BookOrderStatisticsViewDtoInsightsBooksItem[];
  /** One already selected and ordered group of insights. The backend owns the business selection: the array is short, ranked best first, and holds at most one signal of each family, so the frontend renders it as it arrives and never re-ranks or truncates it. */
  orders: BookOrderStatisticsViewDtoInsightsOrdersItem[];
  spendByCurrency: BookOrderStatisticsViewDtoInsightsSpendByCurrencyItem[];
};
